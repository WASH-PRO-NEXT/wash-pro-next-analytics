# wash-pro-next-analytics UI

Stage 3 component UI package.

Pages live in `src/pages/` and are loaded by the platform Plugin Loader
(`dashboard/src/core/pluginLoader.tsx`) via the `@builtin/wash-pro-next-analytics` Vite alias.

Host shell APIs are imported as `@dashboard/...`.
